# Source archives must include the installer bundle, documentation and examples.
# No upstream /watch plugin packaging or export exclusions apply to ShortsScope.
* text=auto
*.zip -text
*.jpg -text
*.png -text
